package com.yeskiy.yreview.settings

/**
 * The one name that the plugin shows to a user.
 *
 * A settings page, a color page, a notification group and an intention group all carry this
 * text. The descriptor holds the same text as plain XML, so a change here needs the same
 * change there. A test compares the two.
 */
object ProductName {

    const val TEXT = "Yreview"
}
